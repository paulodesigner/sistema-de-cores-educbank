# Espelha o ambiente de produção: sem isso a top bar mostra a faixa de ambiente
# com o rótulo "undefined" (o NavBar faz t(workspace) com a env vazia).
VITE_EDUCBANK_ENVIRONMENT=Production
VITE_WORKSPACE=Production
VITE_I18N_LOCALE=pt-BR
VITE_I18N_FALLBACK_LOCALE=pt-BR
